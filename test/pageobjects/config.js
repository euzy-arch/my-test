const config = {
    med2des: {
        url: 'http://192.168.241.85/uat/?f=noUserForms&debug=1',
        usernameField: 'input[type="text"]', // Замените на актуальный локатор
        passwordField: 'input[type="password"]', // Замените на актуальный локатор
        loginButton: 'div[cmptype="Button"][name="btnLogin"]', // Замените на актуальный локатор
        username: 'MKV246',
        password: 'Testautor6!'
    }
};

export default config;